{
  "name": "glasswindui-monorepo",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "pnpm --filter preview-demo dev",
    "build": "pnpm -r build",
    "publish": "pnpm -C packages/glasswindui publish --access public"
  }
}
